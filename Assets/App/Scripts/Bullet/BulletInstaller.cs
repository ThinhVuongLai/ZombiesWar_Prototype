using VContainer;
using VContainer.Unity;

namespace ZombiesWar.Bullet
{
    public sealed class BulletInstaller : IInstaller
    {
        public void Install(IContainerBuilder builder)
        {
        }
    }
}
